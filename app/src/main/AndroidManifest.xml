<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Izinkan aplikasi terhubung ke internet & ambil gambar -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrudProduk">

        <!-- Activity lain tidak perlu intent-filter -->
        <activity
            android:name=".AddProdukActivity"
            android:exported="false" />
        <activity
            android:name=".EditProdukActivity"
            android:exported="false" />
        <activity
            android:name=".DeleteProdukActivity"
            android:exported="false" />

        <!-- ✅ Activity yang dibuka pertama kali saat aplikasi dijalankan -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
